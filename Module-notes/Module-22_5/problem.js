// 1. make a function (feetToInch) converte feet to inch

function feetToInch(inch) {
  let feet = inch * 12;
  console.log(feet);
}
feetToInch(5);

// 2. make a function (centimeterToMeter) converte centimeter to meter
function centimeterToMeter(cm) {
  let meter = cm * 100;
  console.log(meter);
}
centimeterToMeter(20);

/* // 3. make a function (pageRequirement) book in input output pages
// book1 == 100page
// book2 == 200page
// book3 == 300page

function pageRequirement() {} */

// 4. array beasFriends . return the largest name of a friend

const bestFriend = [
  "Hunter",
  "Wilson",
  "William",
  "Edwards",
  "Freddie",
  "Richardson",
  "Kevin",
];
for (let i = 0; i < bestFriend.length; i++) {
  const element = bestFriend[i];
  console.log(element);
}

// 5 . array of nums . stop the array if there is a negative number function ( onlyPositive )
